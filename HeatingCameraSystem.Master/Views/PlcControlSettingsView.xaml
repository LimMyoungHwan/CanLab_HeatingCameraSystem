<UserControl x:Class="HeatingCameraSystem.Master.Views.PlcControlSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:loc="clr-namespace:HeatingCameraSystem.Master.Localization"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="1024"
             Background="#0e1321" Foreground="#dee2f7">
    <UserControl.Resources>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="#161b2a"/>
            <Setter Property="BorderBrush" Value="#3b4949"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Margin" Value="6"/>
        </Style>
        <Style x:Key="Head" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#47eaed"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style x:Key="Lbl" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#bac9c9"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0e1321"/>
            <Setter Property="Foreground" Value="#dee2f7"/>
            <Setter Property="BorderBrush" Value="#3b4949"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>
        <Style x:Key="Apply" TargetType="Button">
            <Setter Property="Background" Value="#2f3444"/>
            <Setter Property="Foreground" Value="#47eaed"/>
            <Setter Property="BorderBrush" Value="#3b4949"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
        </Style>
        <Style x:Key="FieldRow" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </UserControl.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Bottom" Background="#1a1f2e" BorderBrush="#3b4949" BorderThickness="0,1,0,0" Padding="14,8">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#bac9c9"/>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <WrapPanel Orientation="Horizontal">
                <Border Style="{StaticResource Card}" Width="360">
                    <StackPanel>
                        <TextBlock Text="{loc:Loc Plc_Connection}" Style="{StaticResource Head}"/>
                        <StackPanel Style="{StaticResource FieldRow}">
                            <TextBlock Text="{loc:Loc Plc_IpAddress}" Style="{StaticResource Lbl}" Width="110"/>
                            <TextBox Text="{Binding PlcIpAddress}" Width="150"/>
                        </StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}">
                            <TextBlock Text="{loc:Loc Plc_Port}" Style="{StaticResource Lbl}" Width="110"/>
                            <TextBox Text="{Binding PlcPort}" Width="80"/>
                        </StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}">
                            <TextBlock Text="{loc:Loc Plc_Station}" Style="{StaticResource Lbl}" Width="110"/>
                            <TextBox Text="{Binding PlcStationNo}" Width="60"/>
                        </StackPanel>
                        <Button Content="{loc:Loc Common_Save}" Style="{StaticResource Apply}" Command="{Binding SavePlcConnectionCommand}" HorizontalAlignment="Left" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}" Width="360">
                    <StackPanel>
                        <TextBlock Text="{loc:Loc Plc_ChamberBbTitle}" Style="{StaticResource Head}"/>
                        <StackPanel Style="{StaticResource FieldRow}">
                            <TextBlock Text="{loc:Loc Plc_TargetTemp}" Style="{StaticResource Lbl}" Width="110"/>
                            <TextBox Text="{Binding TargetTemperature}"/>
                            <Button Content="{loc:Loc Common_Apply}" Style="{StaticResource Apply}" Command="{Binding ApplyTemperatureCommand}"/>
                        </StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}">
                            <TextBlock Text="{loc:Loc Plc_TargetHum}" Style="{StaticResource Lbl}" Width="110"/>
                            <TextBox Text="{Binding TargetHumidity}"/>
                            <Button Content="{loc:Loc Common_Apply}" Style="{StaticResource Apply}" Command="{Binding ApplyHumidityCommand}"/>
                        </StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}">
                            <TextBlock Text="{loc:Loc Plc_HumidityControl}" Style="{StaticResource Lbl}" Width="110"/>
                            <CheckBox IsChecked="{Binding HumidityControl}" Foreground="#dee2f7" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <Button Content="{loc:Loc Common_Apply}" Style="{StaticResource Apply}" Command="{Binding ApplyHumidityControlCommand}"/>
                        </StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}">
                            <TextBlock Text="{loc:Loc Status_Bb1Target}" Style="{StaticResource Lbl}" Width="110"/>
                            <TextBox Text="{Binding BlackBody1Target}"/>
                            <Button Content="{loc:Loc Common_Apply}" Style="{StaticResource Apply}" Command="{Binding ApplyBlackBody1Command}"/>
                        </StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}">
                            <TextBlock Text="{loc:Loc Status_Bb2Target}" Style="{StaticResource Lbl}" Width="110"/>
                            <TextBox Text="{Binding BlackBody2Target}"/>
                            <Button Content="{loc:Loc Common_Apply}" Style="{StaticResource Apply}" Command="{Binding ApplyBlackBody2Command}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}" Width="360">
                    <StackPanel>
                        <TextBlock Text="{loc:Loc Plc_TempRamp}" Style="{StaticResource Head}"/>
                        <StackPanel Style="{StaticResource FieldRow}">
                            <TextBlock Text="{loc:Loc Plc_TargetTemp}" Style="{StaticResource Lbl}" Width="110"/>
                            <TextBox Text="{Binding RampTargetTemperature}"/>
                        </StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}">
                            <TextBlock Text="{loc:Loc Plc_ReachTime}" Style="{StaticResource Lbl}" Width="110"/>
                            <TextBox Text="{Binding RampMinutes}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="{loc:Loc Plc_RampStart}" Style="{StaticResource Apply}" Command="{Binding StartRampCommand}" Margin="0,0,8,0"/>
                            <Button Content="{loc:Loc Common_Stop}" Style="{StaticResource Apply}" Command="{Binding StopRampCommand}" IsEnabled="{Binding IsRamping}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}" Width="360">
                    <StackPanel>
                        <TextBlock Text="{loc:Loc Plc_MotorFan}" Style="{StaticResource Head}"/>
                        <StackPanel Style="{StaticResource FieldRow}">
                            <TextBlock Text="{loc:Loc Plc_ServoSpeed}" Style="{StaticResource Lbl}" Width="130"/>
                            <TextBox Text="{Binding ServoSpeedPercent}"/>
                            <Button Content="{loc:Loc Common_Apply}" Style="{StaticResource Apply}" Command="{Binding ApplyServoSpeedCommand}"/>
                        </StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}">
                            <TextBlock Text="{loc:Loc Status_FanSpeed}" Style="{StaticResource Lbl}" Width="130"/>
                            <TextBox Text="{Binding FanSpeedHz}"/>
                            <Button Content="{loc:Loc Common_Apply}" Style="{StaticResource Apply}" Command="{Binding ApplyFanSpeedCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}" Width="360">
                    <StackPanel>
                        <TextBlock Text="{loc:Loc Plc_AdminSettings}" Style="{StaticResource Head}"/>
                        <StackPanel Style="{StaticResource FieldRow}"><TextBlock Text="{loc:Loc Plc_OverheatLimit}" Style="{StaticResource Lbl}" Width="150"/><TextBox Text="{Binding OverheatLimit}"/></StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}"><TextBlock Text="{loc:Loc Plc_CoolerRoomBoundary}" Style="{StaticResource Lbl}" Width="150"/><TextBox Text="{Binding CoolerRoomBoundary}"/></StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}"><TextBlock Text="{loc:Loc Plc_Cooler2ndBoundary}" Style="{StaticResource Lbl}" Width="150"/><TextBox Text="{Binding Cooler2ndBoundary}"/></StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}"><TextBlock Text="{loc:Loc Plc_CoolerDelay}" Style="{StaticResource Lbl}" Width="150"/><TextBox Text="{Binding CoolerDelayMinutes}"/></StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}"><TextBlock Text="{loc:Loc Plc_BypassBoundary}" Style="{StaticResource Lbl}" Width="150"/><TextBox Text="{Binding BypassBoundary}"/></StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}"><TextBlock Text="{loc:Loc Plc_MfcMin}" Style="{StaticResource Lbl}" Width="150"/><TextBox Text="{Binding MfcMinOutput}"/></StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}"><TextBlock Text="{loc:Loc Plc_MfcMax}" Style="{StaticResource Lbl}" Width="150"/><TextBox Text="{Binding MfcMaxOutput}"/></StackPanel>
                        <StackPanel Style="{StaticResource FieldRow}"><TextBlock Text="{loc:Loc Plc_PairGlassBoundary}" Style="{StaticResource Lbl}" Width="150"/><TextBox Text="{Binding PairGlassBoundary}"/></StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <Button Content="{loc:Loc Common_Load}" Style="{StaticResource Apply}" Command="{Binding LoadAdminCommand}" Margin="0,0,8,0"/>
                            <Button Content="{loc:Loc Common_Save}" Style="{StaticResource Apply}" Command="{Binding SaveAdminCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}" Width="360">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="{loc:Loc Plc_ServoPointsTitle}" Style="{StaticResource Head}"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="{loc:Loc Common_Load}" Style="{StaticResource Apply}" Command="{Binding LoadPointsCommand}" Margin="0,0,8,0"/>
                                <Button Content="{loc:Loc Common_Save}" Style="{StaticResource Apply}" Command="{Binding SavePointsCommand}"/>
                            </StackPanel>
                        </Grid>
                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="60"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{loc:Loc Plc_Point}" Style="{StaticResource Lbl}"/>
                            <TextBlock Grid.Column="1" Text="X" Style="{StaticResource Lbl}"/>
                            <TextBlock Grid.Column="2" Text="Y" Style="{StaticResource Lbl}"/>
                        </Grid>
                        <ItemsControl ItemsSource="{Binding Points}" MaxHeight="360">
                            <ItemsControl.Template>
                                <ControlTemplate TargetType="ItemsControl">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto"><ItemsPresenter/></ScrollViewer>
                                </ControlTemplate>
                            </ItemsControl.Template>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="60"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Index}" Foreground="#dee2f7" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding X}"/>
                                        <TextBox Grid.Column="2" Text="{Binding Y}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </WrapPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
