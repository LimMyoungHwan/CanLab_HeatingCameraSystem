<UserControl x:Class="HeatingCameraSystem.Master.Views.LiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top"
                   Text="Agent 라이브 영상 (NATS 수신) — Agent가 스트리밍하면 카메라별로 표시됩니다."
                   Foreground="#888888" Margin="4,0,0,10"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Cameras}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="8" Padding="8" CornerRadius="6"
                                Background="White" BorderBrush="#DDDDDD" BorderThickness="1">
                            <StackPanel Width="384">
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Margin="2,0,0,6"/>
                                <Border Background="Black" Height="288">
                                    <Image Source="{Binding LiveImage}" Stretch="Uniform"/>
                                </Border>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
