# 4. 설정 파일 레퍼런스


## 4.1 파일 위치

| 파일 | 위치 | 주체 | 자동생성 |
| --- | --- | --- | --- |
| hardware.json | %LOCALAPPDATA%\HeatingCameraSystem\ | Master | 예 |
| data.db | %LOCALAPPDATA%\HeatingCameraSystem\ | Master(LiteDB) | 예(빈 상태) |
| recipe/*.json | %LOCALAPPDATA%\HeatingCameraSystem\recipe\ | Master | 예(레시피 저장) |
| ImageCache | %LOCALAPPDATA%\HeatingCameraSystem\ImageCache\ | Master | 예 |
| agent.json | Agent exe 폴더 | Agent | 예 |
| manager-settings.json | C:\HeatingCameraSystem\Manager\ | AgentManager 설정 | 아니요(없으면 코드 기본값 사용) |
| manager-state.json | C:\HeatingCameraSystem\Manager\ | AgentManager 카메라 상태 | 카메라 상태 저장 시 생성 |

> **[참고]** 레시피는 예전 LiteDB 컬렉션이 아니라 recipe 폴더의 개별 JSON 파일로 저장된다(구버전 DB가 있으면 최초 1회 자동 마이그레이션). data.db에는 촬영 이력·챔버 이력·알람 이력·대시보드 레이아웃·카메라 시리얼/디바이스 정보가 저장된다.

> **[주의]** 설정을 변경할 때는 해당 프로그램을 종료하고 원본 파일을 백업한 뒤 편집한다. JSON에서는 주석과 후행 쉼표를 사용할 수 없으며, 변경 사항은 프로그램을 재시작한 뒤 적용된다. data.db는 프로그램이 관리하므로 직접 편집하지 않는다.


## 4.2 hardware.json — 최상위

| 필드 | 기본값 | 의미 |
| --- | --- | --- |
| SimulationMode | false | true 시 PLC/셔터를 Fake로 대체(카메라 시뮬은 agent.json) |
| DataRetentionDays | 30 | 촬영 이미지·이력 보관 일수(0=정리 안 함) |
| CameraPairings | [] | 카메라-COM 페어링 목록 |
| Plc / Nats / Serial / BlackBody / RecipeEngine | 각 섹션 | 아래 세부 표 참고 |

> **[참고]** 구 매뉴얼과 달리 캡처 보관일은 코드 하드코딩이 아니라 hardware.json의 DataRetentionDays로 조정한다. BlackBody(SR-800N 직접제어)·CameraPairings 섹션도 현재 버전에서 추가되었다.

### Nats

| 필드 | 기본값 | 의미 |
| --- | --- | --- |
| Url | nats://127.0.0.1:4222 | Master가 연결할 NATS 서버 주소 |

### Serial

| 필드 | 기본값 | 의미 |
| --- | --- | --- |
| PortName | COM3 | 시리얼 셔터 포트 |
| BaudRate | 9600 | 통신 속도 |
| DataBits | 8 | 데이터 비트 |
| Parity | None | 패리티 |
| StopBits | One | 정지 비트 |

### CameraPairings[] 요소

| 필드 | 기본값 | 의미 |
| --- | --- | --- |
| CameraUsbParentId | "" | 카메라 USB 부모 장치 식별자 |
| PortName | "" | 카메라와 연결할 COM 포트 이름 |
| ClSerialNumber | null | 카메라링크 시리얼 번호 |
| DisplayName | null | 화면에 표시할 페어링 이름 |


## 4.3 hardware.json — PLC 주요 필드

| 필드 | 기본값 | 의미 |
| --- | --- | --- |
| IpAddress / Port / StationNo | 192.168.1.2 / 2004 / 0 | PLC 연결(XGT FEnet) |
| CpuSeries / UseHexBitIndex | XGB / true | CPU 계열, 비트 인덱스 16진 |
| TempPv | D100 | 챔버 현재 온도(×10) |
| TempSv | D102 | 챔버 제어 온도(×10) |
| TempTarget | D112 | 챔버 최종 목표 온도(×10) |
| HumPv/HumSv | D130/D131 | 챔버 습도(×10) |
| Bb1Pv/Bb1Sv, Bb2Pv/Bb2Sv | D140/142, D150/152 | 흑체1/2 PV·SV(×100) |
| BitTempStart/Stop | M10/M11 | 온도제어 시작/정지 |
| BitErrorReset | P525 | 에러 리셋 모멘터리 비트 |
| BitBuzzerOff | P250 | 부저 OFF 모멘터리 비트 |
| ServoXPos/YPos, XBusy/YBusy | D2540/D2640, D2520.0/D2620.0 | 서보 위치·구동 비트 |
| ServoPointMoveBase / XBase / YBase | P601 / D3010 / D3012 | 포인트 이동 트리거·좌표 워드 |
| PulseHoldMs | 100 | 원터치 비트 ON 유지 시간(ms) |
| CoordinateMoveDelayMs | 1000 | 좌표 직접 이동 시 목표 워드 쓰기 후 트리거 전 지연(ms) |
| BitJogXPlus | P745 | X축 양의 방향 조그 비트 |
| BitJogXMinus | P746 | X축 음의 방향 조그 비트 |
| BitJogYPlus | P725 | Y축 양의 방향 조그 비트(place­holder) |
| BitJogYMinus | P726 | Y축 음의 방향 조그 비트(place­holder) |
| EqCooler1st | M502 | 1단 냉동기 원터치 비트 |
| EqCooler2nd | M503 | 2단 냉동기 원터치 비트 |
| EqCoolerRoom | M504 | 룸 냉동기 원터치 비트 |
| EqBlower1 | M505 | 블로워 1 원터치 비트 |
| EqBlower2 | M506 | 블로워 2 원터치 비트 |
| EqChiller | P410 | 칠러 원터치 비트 |
| EqDoorLock | P411 | 도어락 원터치 비트 |
| EqLighting | D280.0 | 조명 원터치 비트 |
| EqPairGlass | P370 | 페어글라스 원터치 비트 |
| AdminOverheatLimit | D4004 | 과열 상한(×10℃) |
| AdminCoolerRoomBoundary | D1910 | 룸 냉동기 경계값(×10℃) |
| AdminCooler2ndBoundary | D1920 | 2단 냉동기 경계값(×10℃) |
| AdminCoolerDelay | D78 | 냉동기 지연(분) |
| AdminBypassBoundary | D1940 | 바이패스 경계값(×10℃) |
| AdminMfcMinOutput | D1960 | MFC 최소 출력(×10%RH) |
| AdminMfcMaxOutput | D1950 | MFC 최대 출력(×10%RH) |
| AdminPairGlassBoundary | D1930 | 페어글라스 경계값(×10℃) |

스케일: 온·습도 쓰기 value×10 → short, 읽기 short/10. 흑체는 ×100.

> **[주의]** 다음 값은 일반 기본값이 아니라 소스 주석에 placeholder로 표시된 임의값이다: ServoSpeedPercent(D2560), BitEmergencyStop(M2000), BitJogYPlus(P725), BitJogYMinus(P726), ServoPointYBase(D3012). 실제 비트/워드 확인 후 교체한다.


## 4.4 hardware.json — BlackBody / RecipeEngine

| 필드 | 기본값 | 의미 |
| --- | --- | --- |
| BlackBody.Enabled | false | true 시 PLC 경유 대신 SR-800N 직접제어 |
| BlackBody.Simulated | false | 물리 장비 없이 인메모리 SR-800N 구동 |
| BlackBody.SimulatedRampCelsiusPerSecond | 5.0 | 시뮬레이션 현재 온도가 목표에 수렴하는 속도(℃/초) |
| BlackBody.InterMessageDelayMs | 50 | SR-800N 메시지 사이의 간격(ms) |
| BlackBody.ReadTimeoutMs | 1500 | SR-800N 응답 대기 제한시간(ms) |
| BlackBody.Units[] | COM4/COM5 | 유닛별 연결 설정 |
| RecipeEngine.TemperatureTolerance | 0.5 | 챔버·흑체 안정화 허용오차(℃) |
| RecipeEngine.CaptureResultTimeoutSeconds | 30 | 캡처 결과 대기 제한시간(초) |
| RecipeEngine.RampStepIntervalSeconds | 60 | 온도 램프 SV 갱신 간격(초) |

### BlackBody.Units[] 요소

| 필드 | 기본값 | 의미 |
| --- | --- | --- |
| ConnectionType | Serial | 연결 방식(Serial 또는 Ip) |
| PortName | COM4 | Serial 연결 시 COM 포트 |
| BaudRate | 115200 | Serial 통신 속도 |
| DataBits | 8 | Serial 데이터 비트 |
| Parity | None | Serial 패리티 |
| StopBits | One | Serial 정지 비트 |
| IpAddress | 192.168.1.100 | Ip 연결 시 장치 주소 |
| Port | 5000 | Ip 연결 시 TCP 포트 |

> **[참고]** 현재 버전은 위 RecipeEngine 3개 값을 모두 hardware.json에서 읽어 실제로 적용한다(구 매뉴얼의 '하드코딩/미사용' 설명은 더 이상 유효하지 않음). RampStepIntervalSeconds는 온도 램프 기능과 함께 추가되었다.


## 4.5 agent.json

| 필드 | 기본값 | 의미 |
| --- | --- | --- |
| AgentId | "" | 토픽 라우팅 키(권장 Agent_<숫자>). 값이 비어 있으면 Agent 시작 시 머신명을 지정한다. |
| CameraIndex | 0 | OpenCV 카메라 인덱스 + 토픽 매칭 키 |
| NatsUrl | nats://127.0.0.1:4222 | NATS 주소 |
| StoragePath | ImageStorage | 캡처 저장 폴더(상대=exe 기준) |
| HeartbeatIntervalSeconds | 5 | 상태 발행 주기 |
| SimulationMode | false | true 시 합성 이미지 |
| LogPath | "" | 로그 폴더. 빈 문자열이면 Agent exe 폴더의 logs 사용 |
| CameraModel | null | 지정 시 CameraModels\{CameraModel}.json에서 해상도를 읽고, null이면 카메라 기본 해상도 사용 |


## 4.6 카메라 ↔ Agent ↔ NATS 매핑(중요)

레시피 스텝의 CameraIndex는 master.cmd.capture.Agent_{CameraIndex} 토픽으로 발행된다. 즉 수동 Agent의 AgentId는 Agent_<CameraIndex> 형태여야 명령을 받는다. Manager가 {PCId}_{해시8} 형식 AgentId를 부여한 경우에는 스텝의 CameraAlias에 해당 카메라 Alias를 적어 DB 조회로 라우팅한다.

| 스텝 CameraIndex | 발행 토픽 | 받아야 할 AgentId |
| --- | --- | --- |
| 0 | master.cmd.capture.Agent_0 | Agent_0 |
| 1 | master.cmd.capture.Agent_1 | Agent_1 |


## 4.7 AgentManager 설정 및 상태

### manager-settings.json

AgentManager 시작 설정이다. 파일이 없으면 코드 기본값을 사용한다.

| 필드 | 기본값 | 의미 |
| --- | --- | --- |
| PCId | 머신명 | Manager가 실행되는 PC 식별자 |
| NatsUrl | nats://127.0.0.1:4222 | NATS 서버 주소 |
| SimulateEnumeration | false | true 시 실제 USB 카메라 대신 가상 카메라 2대 열거 |
| SimulateAgentMode | false | true 시 Agent를 합성 이미지 모드로 실행 |
| LogRetentionDays | 7 | Manager 로그 보관 일수 |
| WarnAlertEnabled | false | 경고 알림 사용 여부 |
| InstallRoot | C:\HeatingCameraSystem | 설치 루트 |
| AgentExePath | C:\HeatingCameraSystem\Agent\HeatingCameraSystem.Agent.exe | 콘솔 Agent 실행 파일 경로 |
| AgentUiExePath | C:\HeatingCameraSystem\AgentUI\HeatingCameraSystem.AgentUI.exe | AgentUI 실행 파일 경로 |

### manager-state.json

AgentManager가 발견한 카메라와 승인 상태를 저장하는 파일이다. 프로그램이 자동으로 관리한다.

| 필드 | 기본값 | 의미 |
| --- | --- | --- |
| PCId | 머신명 | 상태를 소유한 PC 식별자 |
| Cameras | [] | 카메라 상태 항목 목록 |

### manager-state.json Cameras[] 요소

| 필드 | 기본값 | 의미 |
| --- | --- | --- |
| HardwareId | "" | 카메라 하드웨어 식별자 |
| AgentId | "" | 카메라에 배정된 Agent 식별자 |
| Alias | "" | 카메라 별칭 |
| OpenCvIndex | 0 | OpenCV 카메라 인덱스 |
| StoragePath | "" | 캡처 저장 경로 |
| IsApproved | false | 카메라 승인 여부 |
| FirstSeen | 날짜/시간 기본값 | 최초 발견 시각 |
| LastSeen | 날짜/시간 기본값 | 마지막 발견 시각 |
| RestartFails | 0 | Agent 재시작 실패 횟수 |
| IsDisabled | false | 카메라 비활성화 여부 |