# 02-시작하기 — agy 검토 (Round 1)

## 종합 평가
- 수정 필요도: 하
- 한 줄 요약: 매뉴얼 대상 독자(일상 운전자)의 눈높이에 맞춰 실제 WPF Master 화면 구조 및 기능과 정확히 부합하게 작성되었으며, 약간의 알람 안내 및 회색 점 점검 대처 문구 추가만으로 완성도를 극대화할 수 있습니다.

## 수정 필요 항목
1. [2.1 프로그램 실행과 첫 화면] NATS·PLC 연결 실패 시 "알람으로 알려준다"는 표현이 다소 모호함 -> 연결 실패 시 발생한 알람이 좌측 하단 알람 목록에 즉시 표시된다는 지점(알람 수신 위치)을 명시하도록 수정 제안.
2. [2.3 정상 상태 확인] 카메라/Agent의 회색 점(응답 없음, 15초 이상) 발생 시 운전자가 취해야 할 기본 점검 사항(Agent PC 전원 상태, 네트워크 및 agent.json 설정 등)을 직관적으로 보완 제안.

## 누락/추가 제안
- **비상정지 버튼 역할 명시:** 2.2절 하단 비상정지 버튼에 긴급 상황 조치 기능 설명을 보완하고, 필요 시 비상 정지 후 복구 절차 관련 연결 안내를 제공할 것을 제안합니다.
- **언어 전환 팁:** 2.2절 언어 선택(한국어/English) 변경 시 화면 텍스트가 즉시 다국어로 반영된다는 점을 팁으로 명시하면 편리합니다.

## 이미지 자리 검토
- **[그림 2] 적절** — 프로그램 최초 실행 시 대시보드 및 좌측 전체 메뉴 배치를 한눈에 직관적으로 보여주어 초기 적응에 매우 효과적입니다.
- **[그림 3] 적절** — 좌측 메뉴의 7개 항목뿐만 아니라 하단 공용 제어 버튼(부저 OFF, 에러 리셋, 원점 복귀), PLC 상태등, 알람 목록, 비상정지 버튼까지 통합 캡처를 지정하여 UI 구성 요소를 명확히 파악할 수 있습니다.

## (선택) 수정 제안 전문

# 2. 시작하기


## 2.1 프로그램 실행과 첫 화면

운영 PC 바탕화면(또는 시작 폴더)의 실행 아이콘으로 Master를 실행한다. 실행하면 아래와 같은 메인 창이 열리고, 자동으로 NATS·PLC 연결을 시도한다(연결에 실패하더라도 화면은 정상적으로 뜨며, 발생한 알람은 좌측 하단 알람 목록에 즉시 표시된다).

> 📷 **[그림 2] Master 메인 창(최초 실행)**
> - **캡처 대상:** 프로그램 실행 직후의 전체 창
> - **화면/상태:** 대시보드가 기본으로 열린 상태. 좌측 메뉴 전체가 보이도록 캡처
> - **표시 영역:** 창 전체


## 2.2 화면 구성 — 좌측 메뉴

왼쪽 세로 메뉴에서 화면을 전환한다. 맨 위 언어 선택(한국어/English — 선택 시 즉시 실시간 전환) 아래로 7개의 화면이 순서대로 배치된다.

| 순서 | 메뉴 | 무엇을 하는 화면 |
| --- | --- | --- |
| 1 | 대시보드 | 챔버·흑체·서보·장비 상태와 카메라 영상, 레시피 실행을 한 화면에서 |
| 2 | 레시피 편집기 | 촬영 순서표(레시피) 작성·수정, 스텝별 좌표/온도/카메라 지정 |
| 3 | 이력 조회 | 촬영 이력·챔버 이력·알람 이력 조회, CSV 내보내기 |
| 4 | Agent 설정 | 카메라 PC 원격 설정(탭1) / 카메라 승인·관리(탭2) |
| 5 | PLC 상태 | PLC의 온습도·서보·입출력·알람을 읽기 전용으로 모니터링 |
| 6 | PLC 설정 | PLC 연결, 흑체 연결, 관리자 한계값, 서보 포인트 좌표 설정 |
| 7 | 수동 조작 | 챔버·서보·흑체·장비·카메라를 직접 수동으로 조작 |

메뉴 아래쪽에는 화면과 무관하게 항상 표시되는 공용 버튼과 알람 영역이 있다.

- **PLC 부저 OFF / PLC 에러 리셋 / PLC 원점 복귀:** 자주 쓰는 PLC 즉시 명령
- **PLC 연결 표시등:** 초록(연결)/빨강(끊김)
- **알람 목록:** 실시간 알람이 최신순으로 쌓임. 등급 필터(전체/정보/경고/오류), 각 알람 옆 ✕로 개별 삭제
- **비상정지:** 하단의 큰 빨간 버튼 (긴급 상황 시 즉시 장비 정지)

> 📷 **[그림 3] 좌측 메뉴 패널**
> - **캡처 대상:** 좌측 세로 메뉴 전체(언어 선택 + 7개 메뉴 + 하단 공용 버튼 + 알람 목록 + 비상정지)
> - **화면/상태:** 알람이 1건 이상 표시된 상태면 더 좋음
> - **표시 영역:** 창 왼쪽 세로 영역


## 2.3 정상 상태 확인

- **PLC 표시등:** 좌측 메뉴 하단 PLC 표시등이 초록색이면 PLC 통신 연결 정상
- **카메라(Agent) 상태:** 대시보드 우측 카메라/Agent 패널에 카메라가 초록 점으로 표시되면 해당 카메라 준비 완료 (5초 이내 하트비트 수신)
- **회색 점 (응답 없음):** 15초 이상 하트비트 미수신 상태 — 해당 Agent PC 전원 켜짐 여부, 네트워크 및 Agent 프로그램 실행 상태 확인 필요
